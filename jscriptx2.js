// ---------------------
// Define a function max() that takes two numbers as arguments and returns the largest of them. Use the if-then-else construct available in JavaScript.
// ---------------------

function max(1,2){
	if (2>1) {return "Two is the largest number."};

	if-then-else (2<1)	{return "This is incorrect."}

    //...
}

// ---------------------
// Define a function maxOfThree() that takes three numbers as arguments and returns the largest of them.
// ---------------------

function maxOfThree(4,6,8){ 
	if (8>6) {return "8 is the largest"};
	if (8>4) {return "8 is the largest"};
	else {return "false"};
    //...
}

// ---------------------
// Write a function that takes a character (i.e. a string of length 1) and returns true if it is a vowel, false otherwise.
// ---------------------
var char=("b")
var vowel=["a","e","i","o","u"]
var consonant=["b","c","d","f","g","h","j","k","l","m","n","p","q","r","s","t","v","w","x","y","z"]
function isVowel(char , vowel , consonant)
{
	if (char === vowel ) {return "true"};
	if (char === consonant) {return "false"};
	
    
}

// ---------------------
// Write a function translate() that will translate a text into "Rovarspraket". That is, double every consonant and place an occurrence of "o" in between. For example, translate("this is fun") should return the string "tothohisos isos fofunon".
// ---------------------
var consonant=["b","c","d","f","g","h","j","k","l","m","n","p","q","r","s","t","v","w","x","y","z"]
var phrase= "rovarspraket"
function rovarspraket(phrase , consonant){ 
	for (var consonant = 0; i < Things.length; i++) {
		Things[i]
	}
    //...
}


// ---------------------
// Define a function reverse() that computes the reversal of a string. For example, reverse("jag testar") should return the string "ratset gaj".
// ---------------------
var word="iron yard"
function reverse(word){ return reverse
	console.log
	
   
};